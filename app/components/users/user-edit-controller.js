
    // userEditCtrl assigning a user to event (add event_id element to user element)

    // user Service.loadById($routeParams.userId).success(function(data){
    //     $scope.user = data;
    // });

    // $scope.assignUserToEvent = function(eventId){
    //     eventService.updateEvent($scope.user._id, $scope.user).then(function(){
    //         $scope.user.event_id = eventId;
    //     });
    // };

    // $scope.event.event_id = 1, 2, 3, or 4 (any one of event object ids)
